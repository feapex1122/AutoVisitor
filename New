import random
import string

def generate_short_code(length):
    characters = string.ascii_letters + string.digits
    short_code = ''.join(random.choice(characters) for i in range(length))
    return short_code

# Example: Generate a 6-character short code
short_code = generate_short_code(6)
print("Random short code:", short_code)
